name: Continuous deployment Api

on:
  workflow_dispatch:
  push:
    branches:
      - master
    paths:
      - "src/Domain/**"
      - "src/Application/**"
      - "src/ToDoAssistant.Application/**"
      - "src/CookingAssistant.Application/**"
      - "src/Accountant.Application/**"
      - "src/Persistence/**"
      - "src/ToDoAssistant.Persistence/**"
      - "src/CookingAssistant.Persistence/**"
      - "src/Accountant.Persistence/**"
      - "src/Utility/**"
      - "src/Infrastructure/**"
      - "src/ToDoAssistant.Infrastructure/**"
      - "src/CookingAssistant.Infrastructure/**"
      - "src/Accountant.Infrastructure/**"
      - "src/Api/**"

env:
  REGISTRY_URL: ${{ secrets.REGISTRY_URL }}
  REGISTRY_IMAGE: ${{ secrets.REGISTRY_IMAGE }}
  REGISTRY_USERNAME: ${{ secrets.REGISTRY_USERNAME }}
  REGISTRY_TOKEN: ${{ secrets.REGISTRY_TOKEN }}

jobs:
  deploy:
    name: Build and push images to Docker Hub
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build Api image
        run: docker build -f ./src/Api/Dockerfile . --tag ${{ env.REGISTRY_URL }}/${{ env.REGISTRY_IMAGE }}:api-latest

      - name: Login to Registry
        uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY_URL }}
          username: ${{ env.REGISTRY_USERNAME }}
          password: ${{ env.REGISTRY_TOKEN }}

      - name: Publish Api image
        run: docker push ${{ env.REGISTRY_URL }}/${{ env.REGISTRY_IMAGE }}:api-latest
